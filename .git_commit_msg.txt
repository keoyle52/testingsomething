fix: strictly follow SosoValue API URLs and implement robust 429 circuit breaker

- Updated sosoValueClient to correctly route requests:
  - ETF endpoints now use `https://api.sosovalue.xyz` (as per gitbook)
  - News/Coins endpoints use `https://openapi.sosovalue.com`
- Added 60-second circuit breaker lock for 429 errors. This prevents
  application from continuously bombarding the rate-limited endpoints.
- Updated EtfTracker.tsx to use Promise.allSettled() so one failed request
  (like a rate-limited metric) doesn't crash the entire screen.
