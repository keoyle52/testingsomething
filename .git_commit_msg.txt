chore: add vercel.json to fix SPA routing 404s

- Added vercel.json with a rewrite rule to map all paths to index.html
- This resolves the issue where refreshing on a React Router route (e.g. /settings) returns a 404 error on Vercel
