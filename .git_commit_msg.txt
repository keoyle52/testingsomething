feat: switch Copy Trader from open orders to executed trades

- Copy Trader now polls `/accounts/:addr/trades` instead of `/orders`
- This ensures Market Orders and instantly filled Limit Orders are instantly caught and copied
- All copied orders now execute as forced Market Orders (type=2) to guarantee immediate fill
